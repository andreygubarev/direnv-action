name: 'direnv-action'
description: 'Set up direnv'
author: 'andreygubarev'

branding:
  icon: 'heart'
  color: 'red'

inputs:
  version:
    description: 'The version of direnv to install'
    required: false
    default: 'latest'

runs:
  using: node20
  main: dist/index.js
